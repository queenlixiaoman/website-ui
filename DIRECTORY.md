# 项目目录结构说明

本文档详细说明了 `hh-website` 项目的目录结构和各文件/文件夹的用途。

## 📁 根目录结构

```
hh-website/
├── dockerImage/          # Docker 容器化部署相关文件
├── node_modules/         # 项目依赖包（自动生成，不提交到版本控制）
├── public/               # 静态资源目录
├── scripts/              # 构建脚本
├── src/                  # 源代码目录
├── eslint.config.mjs     # ESLint 配置文件
├── jsconfig.json         # JavaScript 路径别名配置
├── next.config.mjs       # Next.js 配置文件
├── package.json          # 项目依赖和脚本配置
├── README.md             # 项目说明文档
└── yarn.lock            # Yarn 依赖锁定文件
```

## 📂 详细目录说明

### 🐳 dockerImage/

Docker 容器化部署相关文件目录。

```
dockerImage/
├── bash.sh              # Bash 脚本
├── build.sh              # Docker 镜像构建脚本
├── deploy.sh             # Docker 部署脚本
├── Dockerfile            # Docker 镜像构建文件
└── README.md             # Docker 部署说明文档
```

### 📦 public/

静态资源目录，所有放在此目录下的文件可以通过 `/文件名` 直接访问。

```
public/
└── assets/               # 静态资源文件夹
    ├── imgs/             # 图片资源
    │   └── room1.png     # 示例图片
    └── styles/           # 公共样式文件（如果需要）
        ├── animations.css # 动画样式
        └── tokens.css     # 设计令牌（颜色、间距等）
```

### 🔧 scripts/

构建和部署相关的脚本文件。

```
scripts/
└── buildTime.js          # 构建时间记录脚本
```

### 💻 src/

源代码主目录，所有开发代码都在此目录下。

```
src/
├── app/                  # Next.js App Router 页面目录
├── components/           # React 组件目录
├── config/               # 配置文件目录
├── http/                 # HTTP 请求相关
└── lib/                  # 工具函数库（可选）
```

#### 📄 src/app/

Next.js App Router 的页面和布局文件。

```
src/app/
├── favicon.ico           # 网站图标
├── globals.css           # 全局样式文件
├── layout.js             # 根布局组件（所有页面的公共布局）
├── page.jsx              # 首页组件
├── page.module.css       # 首页样式文件
└── neighborhood/         # 社区/邻里页面路由
    ├── page.jsx          # 社区页面组件
    └── page.module.css   # 社区页面样式
```

**文件说明：**

- `layout.js`: 根布局，包含全局字体、元数据定义，所有页面都会套用此布局
- `page.jsx`: 首页（路由 `/`）的组件
- `globals.css`: 全局样式，包含 CSS 变量、基础重置等
- `neighborhood/`: 动态路由，对应 `/neighborhood` 页面

#### 🧩 src/components/

React 组件目录，按功能分类组织。

```
src/components/
├── common/               # 通用组件（可在多个页面复用）
│   ├── AccessibilityWidget/    # 无障碍访问组件
│   │   └── index.jsx
│   ├── ChatWidget/              # 聊天插件组件
│   │   └── index.jsx
│   ├── Footer/                  # 页脚组件
│   │   ├── index.jsx
│   │   └── index.module.css
│   ├── Header/                  # 页头/导航栏组件
│   │   ├── index.jsx
│   │   └── index.module.css
│   └── LayoutShell/             # 布局外壳组件（包含 Header 和 Footer）
│       ├── index.jsx
│       └── index.module.css
└── features/             # 功能组件（特定页面使用）
    ├── HomeFloorPlans/          # 首页户型展示组件
    │   ├── index.jsx
    │   └── index.module.css
    ├── NeighborhoodMap/         # 社区地图组件
    │   ├── index.jsx
    │   └── index.module.css
    └── NeighborhoodSidebar/     # 社区侧边栏组件
        ├── index.jsx
        └── index.module.css
```

**组件分类说明：**

- `common/`: 通用组件，如 Header、Footer、LayoutShell 等，可在多个页面复用
- `features/`: 功能组件，如 HomeFloorPlans、NeighborhoodMap 等，通常只在特定页面使用

**组件命名规范：**

- 每个组件一个文件夹
- 组件主文件命名为 `index.jsx`
- 样式文件命名为 `index.module.css`（使用 CSS Modules）

#### ⚙️ src/config/

项目配置文件目录，采用模块化配置管理。

```
src/config/
├── common.js             # 公共配置（Header、Footer 等）
├── floorPlans.js         # 户型相关配置
├── home.js               # 首页配置
├── index.js              # 配置入口文件（统一导出）
└── neighborhood.js       # 社区页面配置
```

**配置说明：**

- `index.js`: 配置入口，统一导入并导出所有配置模块
- `common.js`: 公共配置，如导航链接、联系信息等
- `home.js`: 首页特定配置
- `floorPlans.js`: 户型数据配置
- `neighborhood.js`: 社区页面配置

#### 🌐 src/http/

HTTP 请求相关工具和配置。

```
src/http/
├── api.js                # API 请求封装
└── config.js             # HTTP 请求配置
```

#### 📚 src/lib/

工具函数库目录（可选，当前为空，可根据需要添加工具函数）。

## 📝 配置文件说明

### package.json

项目依赖和脚本配置。

**主要脚本：**

- `yarn dev`: 启动开发服务器（端口 7003）
- `yarn build`: 构建生产版本
- `yarn start`: 启动生产服务器
- `yarn lint`: 代码检查
- `yarn build:docker`: 构建 Docker 镜像并部署
- `yarn build:deploy`: 本地打包部署至工作站

### next.config.mjs

Next.js 配置文件，包含：

- API 代理配置（`rewrites`）
- CORS 头部配置（`headers`）
- 构建优化配置
- 实验性功能配置

### jsconfig.json

JavaScript 路径别名配置，支持：

- `@/` 别名指向 `src/` 目录
- 改善编辑器的自动补全和路径跳转

### eslint.config.mjs

ESLint 代码检查配置，用于保持代码质量和一致性。

## 🎨 样式文件说明

### 全局样式

- `src/app/globals.css`: 全局样式，包含 CSS 变量、基础重置等
- `public/assets/styles/tokens.css`: 设计令牌（颜色、间距等 CSS 变量）
- `public/assets/styles/animations.css`: 动画样式定义

## 🔄 数据流说明

1. **配置驱动**: 页面内容通过 `src/config/` 目录下的配置文件管理
2. **组件化**: 页面由多个可复用组件组合而成
3. **样式分离**: 所有样式写在独立的 CSS Module 文件中，避免行内样式

## 📌 开发规范

1. **组件命名**: 使用 PascalCase，文件夹名与组件名一致
2. **文件命名**:
   - 组件文件：`index.jsx`
   - 样式文件：`index.module.css`
   - 页面文件：`page.jsx`
3. **样式规范**:
   - 使用 CSS Modules，避免全局样式
   - 不使用行内样式（`style={{}}`）
   - 不使用 `!important`（通过选择器特异性控制优先级）
4. **配置管理**: 所有可配置内容放在 `src/config/` 目录下
5. **静态资源**: 图片等静态资源放在 `public/` 目录下
